# 피커 휠 목차 — 나중에 다듬을 것 (TODO)

> 대상: `feat/picker-toc` 브랜치 — 인트로 우측 PROJECTS 목차(iOS 피커 휠)
> 작성: 2026-06-12 / 커밋 `221f966`
> 관련 파일: `js/intro-toc.js`, `js/deck.js`, `js/intro-toc-preview.js`, `css/style.css`(라이브 셸·프레임·트랙·슬롯 832~1060 근처), `index.html`(`.intro-list--live` 736~743)

## 사용자가 말한 것 (2026-06-12)
- [ ] **위치가 이상함** — (구체적으로 무엇이 어디로 어긋나는지 추후 메모: 목차 전체 위치? 프레임? 카드 정렬? 라벨?)
- [ ] **디자인이 이상한 부분 있음** — (어느 요소인지 추후 메모: 프레임 글래스? 카드? 기울임? 색?)

## 의심 지점 (팀 리뷰에서 나온 단서 — 위 항목 좁힐 때 참고)
- [ ] **프레임 글래스 대비**: 8개 프로젝트 배경(특히 밝은 배경 = 자린고비 등) 위에서 흰 글래스 프레임/텍스트가 묻히지 않는지. 테마별(`[data-project]`) 대비 점검.
- [ ] **프레임/카드 정렬**: `--frame-h`·`--row`는 JS 실측(measureRow)인데, 폰트 로드 지연 시 첫 프레임에서 한 칸 어긋날 수 있음. 프레임 중심 vs 가운데 카드 중심 정렬 재확인.
- [ ] **목차 고정 위치**: `.intro-list--live`는 `position:fixed; right:var(--pad); top:clamp(200px,30vh,340px)`. 뷰포트/해상도별로 위치가 의도와 맞는지(특히 좌측 정보·힌트와 겹침 여부).
- [ ] **휠 깊이 기울기 방향**: 위 칸은 윗면, 아래 칸은 아랫면이 앞으로(rotateX 부호) — 실제로 자연스러운지.
- [ ] **인접 롤 타이밍**: 목차 인접 굴림 0.42s가 패널 슬라이드 0.8s보다 빨리 멈춰 "목차만 먼저 안착"으로 어색한지. 어색하면 인접 dur 0.55~0.6s 후보.
- [ ] **viewport 높이/마스크**: `height: calc(5*--row - 8px)` + 가장자리 mask 페이드가 5칸을 깔끔히 보여주는지(잘림/여백).

## 데스크톱/모바일 분기 (현재 의도 — 바꿀 때 참고)
- rotateX·perspective·blur = 데스크톱 ≥1440px 전용. 모바일/태블릿(901~1439px)은 scale+opacity만. ≤900px는 목차 숨김.
- track `will-change`는 롤 중에만(JS 토글). 상시 X.

## 향후 함정
- `PROJECTS` 개수 바뀌면 `css` 트랙 `margin-top: calc(-2 * --row)`의 칸수(= `HALF - RING_HALF`)도 같이 맞춰야 함(현재 8개 기준).

## 폴백
- 피커가 마음에 안 들면 → `feat/live-toc-cascade`(이전 reel/roll, hold 0.5s)로 복귀 가능.
